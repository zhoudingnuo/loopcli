# Bug 修复完成

## 已修复

### 1. 日志轮转 Bug ✅
- 问题：raw.log 被占用时导致 PermissionError
- 修复：添加异常处理，文件被占用时跳过轮转

### 2. WebUI Main Agent 状态显示 Bug ✅
- 问题：API 返回 `active` 状态，但前端只处理 `running`/`idle`
- 修复：添加对 `active` 状态的处理，显示为"活跃"

## 关于花费显示
花费功能正常工作。显示"本轮无新增 token 消耗"是因为增量差值为0。如需显示累计花费，可进一步优化。
