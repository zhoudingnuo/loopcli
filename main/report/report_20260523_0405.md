# WebUI 布局重组完成

已完成微信指令的UI调整：

**主页（新增）**：状态栏 + 长期任务 + 用量统计 + Agent状态分布圆环 + 任务进度条
**Agent页**：搜索栏 + 创建/刷新/导出按钮 + Agent卡片网格
**圆环图修复**：改为 viewBox + width:100% 自适应容器宽度

侧边栏导航：主页(1) / Agents(2) / Tasks(3) / Performance(4) / Logs(5) / Screenshots(6) / Settings(7)
