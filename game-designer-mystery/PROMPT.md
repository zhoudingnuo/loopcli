# 任务：撰写中国悬疑解谜游戏 GDD

## 执行步骤

1. 读取 `SOUL.md` 了解身份和任务要求
2. 读取 `memory/tasks.json` 获取任务详情
3. 创建 `D:/game-projects/steam-chinese-mystery/` 目录
4. 撰写完整 GDD，保存到 `D:/game-projects/steam-chinese-mystery/GDD.md`
5. 完成后通知 main agent：在 `D:/loopcli/main/inbox/` 创建 `gdd-mystery-complete.md`
6. 更新 `memory/tasks.json` 标记任务完成

## 约束
- 禁止 AskUserQuestion
- 禁止运行 python 或 npm
- 禁止修改 loopcli 核心文件
