# P0 安全问题修复报告

**执行时间：** 2026-05-21 07:00:00
**执行者：** engineering-frontend-developer
**任务ID：** 2

---

## 修复清单

### server.py (D:\loopcli\main\webui\server.py)

| # | 问题 | 修复方式 | 状态 |
|---|------|----------|------|
| 1 | `_handle_agent_start` 使用 `shell=True` | 删除 shell=True 分支，统一通过 stdin 传入 prompt 内容 | ✅ |
| 2 | 所有 agent_id 端点存在路径遍历漏洞 | 新增 `_safe_agent_path()` 校验函数，覆盖 SSE 日志、日志API、Agent启动、派发、消息发送、启用/禁用 共 6 个端点 | ✅ |
| 3 | `--dangerously-skip-permissions` 无鉴权 | `_handle_loopcli_dispatch` 添加 API Key 校验（环境变量 LOOPCLI_API_KEY） | ✅ |
| 4 | `_handle_loopcli_restart` 读取 body 在 stop 之后 | 调整为先读 body 再 stop | ✅ |

### run.py (D:\loopcli\run.py)

| # | 问题 | 修复方式 | 状态 |
|---|------|----------|------|
| 5 | `run_agent` 使用 `shell=True` | 改为列表形式 `[CLAUDE, "--print", "--verbose", "--output-format", "stream-json", "--dangerously-skip-permissions"]` | ✅ |
| 6 | `log_file`/`agent_log` 异常时未关闭 | 用 `try/finally` 包裹，确保文件句柄释放 | ✅ |
| 7 | `state.json` 并发写入无保护 | 使用 `msvcrt.locking` 文件锁保护 read-modify-write 操作 | ✅ |
| 8 | git push URL 暴露 token | 改用 GIT_ASKPASS + GH_TOKEN 环境变量，token 不再出现在命令行参数中 | ✅ |
| 9 | `open(token_file)` 未用 with 语句 | 改为 `with open(token_file, "r") as f:` | ✅ |

## 安全验证

- `shell=True` 全部移除（server.py + run.py）
- 路径遍历：`_safe_agent_path` 检查 `/`, `\`, `..` 并 resolve 验证
- API 鉴权：dispatch 端点检查 X-API-Key 头或 key 查询参数
- 文件锁：Windows msvcrt.locking 保护 state.json 并发写入
- Token 保护：GIT_ASKPASS 方式避免命令行暴露
