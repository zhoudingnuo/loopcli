# 任务 #3 结果：为 server.py 和 run.py 编写单元测试

## 完成时间
2026-05-21 08:00:21

## 产出
- `D:/loopcli/main/tests/test_server.py` — 41 个测试
- `D:/loopcli/main/tests/test_run.py` — 23 个测试
- 合计 **64 个测试，全部通过**

## 测试覆盖范围

### server.py (41 tests)
| 模块 | 测试数 | 覆盖内容 |
|------|--------|----------|
| `_safe_agent_path` | 7 | 有效路径、空字符串、斜杠/反斜杠遍历、`..` |
| `read_json` / `write_json` | 6 | 文件不存在、损坏 JSON、目录创建、往返验证 |
| `get_recent_lines` | 3 | 不存在文件、尾部 N 行、行数不足 |
| `scan_agents` | 3 | 返回列表、包含 main、必要字段 |
| `GET /api/agents` | 3 | 200 状态码、JSON 列表、包含 main |
| `GET /api/tasks` | 2 | 200 状态码、JSON 列表 |
| `POST /api/tasks` | 3 | 创建成功、缺少 title 400、空 body 400 |
| `POST /api/messages/send` | 5 | 发送到 main、默认 agent、空内容、无效 agent、不存在 agent |
| 路径遍历拒绝 | 3 | logs 端点、messages 端点 |
| `GET /api/logs` | 3 | 200、有效 agent、无效 agent |
| CORS OPTIONS | 1 | 204 + CORS 头 |
| 404 处理 | 1 | 未知 POST 路由 |

### run.py (23 tests)
| 模块 | 测试数 | 覆盖内容 |
|------|--------|----------|
| `get_agent_marker` | 3 | 有效标记、缺失、禁用标记 |
| `is_agent_enabled` | 3 | 启用、禁用、无标记 |
| `discover_agents` | 3 | 发现真实 agents、必要字段、排除禁用 |
| `find_template` | 2 | 已存在模板、不存在模板 |
| `load_agent_state` | 3 | main 状态、fixture、缺失返回 None |
| `load_agent_tasks` | 4 | 加载任务、空任务、缺失、main 真实任务 |
| `cmd_task_inner` | 3 | 无效 agent 退出、真实 agent 添加、ID 自增 |
| Agent 结构检查 | 2 | main 目录、前端 agent 目录 |

## 技术方案
- **框架**: pytest 9.0.3
- **HTTP 客户端**: httpx 0.28.1
- **server.py 测试方式**: 启动真实 ThreadedHTTPServer + httpx Client（端口随机分配）
- **run.py 导入**: 通过预设 sys.argv=["run.py", "list"] 绕过模块级 argparse
- **测试隔离**: 所有写操作的测试均有清理逻辑，不影响现有数据
