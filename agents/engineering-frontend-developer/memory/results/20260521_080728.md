# Task #4: WebUI 多 Agent 统一仪表盘

## 状态
已完成

## 变更摘要

### 后端 (server.py)
1. `scan_agents()` 新增 `task_count`、`task_done`、`task_pending` 字段，每个 Agent 卡片直接展示任务统计
2. 新增 `get_agent_tasks(agent_id)` 函数，按 agent_id 读取对应 tasks.json
3. 新增 `get_all_agent_tasks()` 函数，聚合所有 Agent 的任务（带 `_agent_id` 标记）
4. `GET /api/tasks` 支持 `?agent=<id>` 和 `?agent=__all__` 查询参数
5. 新增 `GET /api/agents/<name>/tasks` 端点

### 前端 (index.html)
1. Agent 卡片新增任务统计（已完成/待处理/总任务数）
2. Agent 卡片支持点击"展开任务"按钮，动态加载并展示该 Agent 的任务列表
3. 任务管理页面新增 Agent 筛选下拉框（main / 所有 Agent / 各个 Agent）
4. 任务管理页面在"所有 Agent"模式下新增 Agent 列列显示
5. 统计栏的"待处理任务"改为从所有 Agent 汇总计算

## 文件修改
- `D:/loopcli/main/webui/server.py` - 后端 API 增强
- `D:/loopcli/main/webui/index.html` - 前端 UI 改造
