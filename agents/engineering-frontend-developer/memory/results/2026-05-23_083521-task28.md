# 任务 #28 三消提审材料补充 — 完成报告

**执行时间**: 2026-05-23 08:35:21
**状态**: 完成

## 交付清单

### 1. 应用图标 (1024x1024)
- 创建 `tools/generate-icon.html` — Canvas 绘制图标，水果主题，可导出为 PNG
- 包含：径向渐变背景、水果主图标、装饰性小水果、星光特效、标题文字
- 用法：浏览器打开 → 点击"下载 PNG" → 保存为 icon.png

### 2. 宣传截图 (3-5张)
- 创建 `tools/generate-screenshots.html` — 5张 1280x720 横屏截图
- 截图内容：主界面、核心玩法、连锁特效、关卡地图、好友PK
- 用法：浏览器打开 → 点击"全部下载" → 逐张保存 PNG

### 3. 隐私政策页面
- 创建 `privacy-policy.html` — 完整的中文隐私政策
- 覆盖：信息收集、使用、存储、第三方服务（穿山甲SDK）、共享、用户权利、未成年人保护、联系方式
- 符合抖音小游戏提审隐私政策要求

### 4. 穿山甲SDK真实接入适配
- 创建 `src/js/ad-config.js` — 广告位ID集中配置文件
  - 提审前只需替换 `adUnitIds` 中的 placeholder 为真实ID
  - 支持开发模式自动降级
  - 可配置：广告频率限制、重试次数、超时时间
- 更新 `src/js/AdManager.js`：
  - 从 ad-config.js 读取广告位ID
  - 新增 onLoad/onError 事件处理
  - 新增广告预加载和自动重试机制
  - SDK初始化失败自动降级到 stub 模式

### 5. JS/CSS 压缩
- 创建 `build.py` 构建脚本
- 压缩效果：JS 总计 181KB → 143KB (21.2% 压缩)
- 输出 `dist/` 目录：0.14 MB（远低于 4MB 限制）
- 包含：JS minify、HTML minify、静态文件复制、包体检查

### 6. 配置文件完整性
- 更新 `game.json`：新增 navigateToMiniProgramAppIdList、permission
- 更新 `project.config.json`：
  - 新增 libVersion (2.25.0)
  - 新增 packOptions.ignore（排除 GDD.md、tools/、results/ 等非必要文件）
  - 新增 condition 字段
- 更新 `index.html`：引入 ad-config.js 在 AdManager.js 之前

## 提审注意事项
- `project.config.json` 的 appid 需要填写抖音开发者后台申请的 AppID
- `ad-config.js` 的 adUnitIds 需要替换为穿山甲后台申请的真实广告位ID
- 应用图标需要打开 generate-icon.html 生成并下载 PNG
- 宣传截图需要打开 generate-screenshots.html 生成并下载
