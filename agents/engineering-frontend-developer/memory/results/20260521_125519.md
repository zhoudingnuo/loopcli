# 任务 #13 路径配置化：消除硬编码路径

## 执行时间
2026-05-21 12:55:25

## 变更摘要

### 1. loopcli_lib.py — LOOPCLI_ROOT 配置化
- **原**: `LOOPCLI_ROOT = Path(r"D:\loopcli")` — 硬编码绝对路径
- **新**: `LOOPCLI_ROOT = _resolve_root()` — 支持环境变量 + 自动检测
  - 优先级: `LOOPCLI_ROOT` 环境变量 > 从 `__file__` 自动推断（向上三级）
  - 自动检测原理: `loopcli_lib.py` 位于 `LOOPCLI_ROOT/main/webui/`，所以 `Path(__file__).resolve().parent.parent.parent` 即为根目录

### 2. run.py — 消除 sys.path.insert 硬编码
- **原**: `sys.path.insert(0, r"D:\loopcli\main\webui")`
- **新**: `sys.path.insert(0, str(Path(__file__).resolve().parent / "main" / "webui"))`
- 基于脚本自身位置动态计算，无需硬编码

### 3. test_run.py — 消除测试硬编码路径
- **原**: `sys.path.insert(0, r"D:\loopcli")`
- **新**: `sys.path.insert(0, str(Path(__file__).resolve().parent.parent.parent))`

### 4. CONFIGURATION.md — 更新文档
- 新增 `LOOPCLI_ROOT` 环境变量说明
- 移除旧表格中的硬编码路径引用

## 向后兼容性
- 未设置 `LOOPCLI_ROOT` 环境变量时，行为与修改前完全一致（自动检测到相同路径）
- 所有 79 个单元测试通过，零回归

## 修改文件列表
1. `D:\loopcli\main\webui\loopcli_lib.py` — LOOPCLI_ROOT 配置化
2. `D:\loopcli\run.py` — sys.path.insert 动态化
3. `D:\loopcli\main\tests\test_run.py` — 测试路径动态化
4. `D:\loopcli\docs\CONFIGURATION.md` — 文档更新
