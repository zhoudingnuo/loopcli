# Task #15: Agent状态分布圆环图自适应

**Date**: 2026-05-23 04:18:04
**Status**: Done

## Changes

Modified `D:/loopcli/main/webui/index.html`:

### 1. drawDonutChart function (line ~1823)
- Removed fixed `max-width:180px` from SVG inline style
- Added `max-width:220px` as CSS class for better scaling range
- Added `flex-wrap:wrap` to wrapper div for responsive wrapping
- Added CSS class names: `donut-chart-wrapper`, `donut-legend`, `donut-legend-item`

### 2. CSS (line ~872) - New responsive rules
- `.donut-chart-wrapper` uses flex-wrap for adaptive layout
- `.donut-legend` uses `flex:1` to fill available space
- At `max-width:480px`: chart and legend stack vertically (column layout), SVG shrinks to 160px, legend takes full width

### Summary
- SVG uses `viewBox` for intrinsic scaling (unchanged) + `width:100%` for fill + `max-width:220px` as upper bound
- Container flex-wrap ensures chart+legend wrap on narrow screens
- CSS media query at 480px switches to vertical stack layout
