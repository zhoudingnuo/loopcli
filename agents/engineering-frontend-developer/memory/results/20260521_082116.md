# 任务 #6 修复单元测试质量缺口 — 结果报告

## 执行时间
2026-05-21 08:21:16

## 任务摘要
修复 code-reviewer 发现的测试质量缺口（评分 7/10 → 10/10）

## 修改内容

### 1. _safe_agent_path null byte 注入测试（4 个新测试）
- `test_null_byte_injection`: `"main\x00"` → None
- `test_null_byte_in_middle`: `"main\x00evil"` → None
- `test_null_byte_at_start`: `"\x00main"` → None
- `test_null_byte_with_suffix`: `"main\x00.sh"` → None
- **同时修复 server.py**: 在 `_safe_agent_path` 中添加 `'\x00' in agent_id` 检查

### 2. API Key 认证测试（6 个新测试）
- `test_create_task_rejects_without_key`: 无 Key → 401
- `test_create_task_rejects_wrong_key`: 错误 Key → 401
- `test_create_task_accepts_valid_key`: 正确 Key → 201
- `test_message_send_rejects_without_key`: 无 Key → 401
- `test_message_send_accepts_valid_key`: 正确 Key → 201
- `test_message_send_key_in_query_params`: Query 参数传递 Key → 201
- **同时修复 server.py**: 在 `_do_POST_impl` 中为 `/api/tasks` 和 `/api/messages/send` 添加 API Key 鉴权

### 3. 文件锁机制测试（3 个新测试）
- `test_concurrent_writes_no_corruption`: 10 线程并发更新，最终计数正确
- `test_lock_produces_valid_json`: 重 contention 下 JSON 不损坏
- `test_single_write_preserves_data`: 单次写入保留所有已有字段

### 4. 测试副作用修复
- `test_create_task_success`: 改用 try/finally 确保清理
- `test_adds_task_to_real_agent`: 改用 try/finally + tasks_before 恢复
- `test_task_id_auto_increments`: 改用 try/finally + tasks_before 恢复

## 修改文件
- `D:/loopcli/main/webui/server.py`: null byte 防护 + 写入端点 API Key 鉴权
- `D:/loopcli/main/tests/test_server.py`: 4 null byte 测试 + 6 API Key 测试 + 副作用修复
- `D:/loopcli/main/tests/test_run.py`: 3 文件锁测试 + 副作用修复 + threading 导入

## 测试结果
**77/77 全部通过** (23.21s)
